% Claim2Value 项目说明文档（北大金融 AI 智能体创新大赛）
% 编译：latexmk -xelatex claim2value_doc.tex
\documentclass[11pt,a4paper]{ctexrep}
\usepackage[margin=2.4cm,top=2.6cm,bottom=2.6cm]{geometry}
\usepackage{xcolor,graphicx,booktabs,array,tabularx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta}
\usepackage[most]{tcolorbox}
\usepackage{fancyhdr,titlesec,enumitem,fontspec,xeCJK,indentfirst}
\usepackage{hyperref}

% ---------- 品牌色 ----------
\definecolor{navy}{HTML}{1E2761}
\definecolor{gold}{HTML}{F0B429}
\definecolor{ice}{HTML}{CADCFC}
\definecolor{ink}{HTML}{212121}
\definecolor{mute}{HTML}{5A6372}
\definecolor{lightbg}{HTML}{F4F7FE}

% ---------- 字体 ----------
\setCJKmainfont{Microsoft YaHei}
\setCJKsansfont{Microsoft YaHei}
\setmainfont{Fira Sans}
\setsansfont{Fira Sans}

\hypersetup{colorlinks=true, linkcolor=navy, urlcolor=navy, citecolor=navy}

% ---------- 标题样式 ----------
\titleformat{\chapter}[block]
  {\normalfont\sffamily\bfseries\color{navy}}
  {\fontsize{40}{44}\selectfont\thechapter\hspace{0.4em}\textcolor{gold}{\rule[-0.25em]{3pt}{1.6em}}\hspace{0.4em}}
  {0pt}{\fontsize{22}{26}\selectfont}
\titlespacing*{\chapter}{0pt}{-10pt}{18pt}
\titleformat{\section}{\normalfont\sffamily\bfseries\large\color{navy}}{\thesection}{0.6em}{}
\titleformat{\subsection}{\normalfont\sffamily\bfseries\normalsize\color{navy}}{\thesubsection}{0.6em}{}

% ---------- 页眉页脚 ----------
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\sffamily\color{mute}Claim2Value · 项目说明文档}
\fancyhead[R]{\small\sffamily\color{mute}北大金融 AI 智能体创新大赛}
\fancyfoot[C]{\small\sffamily\color{mute}\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\color{ice}\hrule width\headwidth height 0.4pt}

% ---------- 盒子（克制使用） ----------
\newtcolorbox{casebox}[1][案例]{colback=lightbg,colframe=navy,
  boxrule=0.7pt,arc=1.5mm,left=8pt,right=8pt,top=6pt,bottom=6pt,
  title={\sffamily\bfseries #1},coltitle=white,colbacktitle=navy}
\newtcolorbox{warnbox}[1][说明]{colback=white,colframe=mute,
  boxrule=0.5pt,arc=1mm,left=8pt,right=8pt,top=6pt,bottom=6pt,
  title={\sffamily\bfseries #1},coltitle=white,colbacktitle=mute}

% 图：居中，与正文同宽
\newcommand{\docfig}[2][0.86]{%
  \begin{center}\includegraphics[width=#1\linewidth]{#2}\end{center}}

\setlist{itemsep=2pt,topsep=4pt}
\setlength{\parindent}{2em}

\begin{document}

% ================= 封面 =================
\begin{titlepage}
\begin{tikzpicture}[remember picture,overlay]
  \fill[navy] (current page.south west) rectangle (current page.north east);
  % 顶部信息
  \node[anchor=north west,text=ice,font=\sffamily\small]
    at ([xshift=2.2cm,yshift=-2.2cm]current page.north west)
    {北大金融 AI 智能体创新大赛 \quad 2026 年 9 月};
  % 标题区左侧金色短尺
  \fill[gold] ([xshift=2.2cm,yshift=9.0cm]current page.south west) rectangle ++(1.3,0.07);
  % 标题与副标题
  \node[anchor=west,text=white,font=\fontsize{44}{52}\selectfont\sffamily\bfseries]
    at ([xshift=2.15cm,yshift=8.2cm]current page.south west) {Claim2Value};
  \node[anchor=west,text=ice,font=\fontsize{17}{22}\selectfont\sffamily]
    at ([xshift=2.2cm,yshift=6.9cm]current page.south west)
    {工业技术 Claim 到金融价值的可信映射 Agent};
  \node[anchor=west,text=ice,font=\sffamily]
    at ([xshift=2.2cm,yshift=5.9cm]current page.south west)
    {机器人关节模组产业链};
  % 底部统计行
  \draw[ice,line width=0.6pt] ([xshift=2.2cm,yshift=3.4cm]current page.south west)
    -- ([xshift=-2.2cm,yshift=3.4cm]current page.south east);
  \node[anchor=west,text=ice,font=\sffamily\small]
    at ([xshift=2.2cm,yshift=2.9cm]current page.south west)
    {51 条结构化 Claim \;·\; 47 条公告原文级验证 \;·\; 140 项回归测试 \;·\; 11 家上市公司};
\end{tikzpicture}
\vfill
\end{titlepage}

% ================= 摘要 =================
\chapter*{摘要}
\addcontentsline{toc}{chapter}{摘要}
\thispagestyle{fancy}

机器人产业链的技术事件每天都在发生：减速器扭矩密度提升、丝杠量产、新订单落地。这些事件值多少钱，目前靠分析师人肉判断，卖方研报的口径也各不相同。

Claim2Value 做的事：把公司的技术宣称逐条对回公告原文验证，并把验证结论传导进估值模型。置信度低的宣称会真实拉低估值假设，口径有问题的数据会被拦在模型之外。最终交付的不是目标价，而是一张「市场隐含预期 ↔ 已验证证据」对照表。以绿的谐波为例：512.96 亿市值隐含的销量预期，是可验证基准假设的 16.7 倍。差多少、差在哪、证据强度够不够，由买方自己判断。

系统目前覆盖 11 家上市公司、51 条结构化 Claim，其中 47 条完成公告原文级验证（页码定位加内容指纹）。140 项回归测试全过，离线可复现。

\tableofcontents

% ================= 第 1 章 =================
\chapter{问题：工程信号与金融决策之间的断层}

机器人关节模组产业链的信息生产端很热闹：减速器厂发新品参数，丝杠厂发订单公告，整机厂发量产计划。信息的消费端是买方的买入、卖出、持有决策。两端之间没有可靠的转换层，断层具体体现在三个地方。

一是人肉翻译。把「额定扭矩 51--130 Nm」翻译成「收入能多多少」，靠分析师手工完成。一条宣称从公告到估值假设，中间没有留痕，复核者无法复算。

二是口径打架。卖方研报对公司 ASP 的假设各自为政，而且常常滞后于现实。绿的谐波是一个现成的例子：券商盈利预测普遍按约 1,500 元/台建模，2024 年年报的实际 ASP 是 1,293 元，2025 年市场口径进一步到 1,099 元。假设被击穿之后，研报的估值结论并没有同步修正。

\docfig{fig_asp.pdf}

三是真假难辨。公司公告、券商研报、媒体转述、行业蓝皮书常常出现在同一张对比表里，但表上看不出哪一格是公告原文、哪一格是媒体转述、哪一格根本没有出处。下面这个案例说明，连头部券商的研报也会出这种错。

\begin{casebox}[口径错位实例：双环传动 SH\_003]
一家头部券商研报写道：「双环传动 2025Q2 减速器及其他业务收入 3.49 亿元，同比增长 35.8\%」。我们在做口径审查时对照了双环 2025 年半年报第 14 页：原文是\textbf{半年累计} 348,990,082.38 元、同比 +35.66\%。研报把半年累计数当成了单季数，增速也改了小数点。拿「Q2 单季」去年化，收入会直接高估一倍。
\end{casebox}

% ================= 第 2 章 =================
\chapter{产品：从 Claim 到估值的可信流水线}

\section{架构}

系统的处理链路如下。验证和因果批判两个环节是一般投研工具没有的：前者决定一条宣称可不可信，后者决定可信的宣称在打几折之后才能进模型。

\begin{center}
\begin{tikzpicture}[
  stage/.style={draw=navy,fill=navy,text=white,rounded corners=1.5mm,minimum width=1.85cm,minimum height=0.95cm,font=\sffamily\small\bfseries,align=center},
  key/.style={draw=gold,fill=gold,text=navy,rounded corners=1.5mm,minimum width=1.85cm,minimum height=0.95cm,font=\sffamily\small\bfseries,align=center},
  arr/.style={-{Stealth[length=2.5mm]},line width=1.2pt,mute}]
\node[stage] (s1) at (0,0) {Claim};
\node[key]   (s2) at (2.15,0) {验证};
\node[stage] (s3) at (4.3,0) {门控};
\node[stage] (s4) at (6.45,0) {工程\\归一化};
\node[stage] (s5) at (8.6,0) {经济\\映射};
\node[key]   (s6) at (10.75,0) {因果\\批判};
\node[stage] (s7) at (12.9,0) {财务\\三情景};
\foreach \a/\b in {s1/s2,s2/s3,s3/s4,s4/s5,s5/s6,s6/s7} \draw[arr] (\a) -- (\b);
\end{tikzpicture}
\end{center}

\section{验证结论如何传导进估值}

多数工具的做法是「验证归验证、财务照算」，验证结论和估值模型互不影响。本产品的做法相反：验证置信度直接调整经济假设，调整后的假设再进模型。一条宣称越不可信，它对估值的影响就越小——这不是展示层面的标注，是计算层面的传导。

每条经济假设带三样东西：证据等级、置信度、provenance 链。从任何一个估值数字出发，能一路回溯到公告原文的具体页码。财务模型的每个输入也分三栏：历史锚点（年报原文）、人工假设（写明设定者和依据）、计算结果。看模型的人能分清哪个数字是披露、哪个数字是情景。

因果批判层处理的是另一类问题：即便宣称属实，「技术到业绩」的因果链也未必成立。它对每个经济假设生成替代解释清单——销量增长里多少是行业共同的 $\beta$，多少是公司自己的 $\alpha$——回答不了的，从置信度里扣掉，扣多少写在输出里。

\section{工程口径}

工程参数进模型之前先做归一化：额定扭矩和峰值扭矩分开、电机本体和关节模组分开、不同减速比档位分开。不可比的参数明确标记为不可比，不参与同业中位数计算。映射规则全部写在 ontology 文件里，代码中不硬编码任何「技术指标到经济变量」的对应关系，换行业就是换一份规则文件。

% ================= 第 3 章（新） =================
\chapter{智能体本体：它如何工作}

\section{输入与输出}

使用者给它一条公司宣称和对应的证据文本，它返回一份可审计的分析报告：验证结论（成立 / 部分成立 / 不成立 / 拒答）、证据等级与置信度、口径疑点、传导后的三情景估值，以及每个数字的回溯路径。

\section{双轨验证与降级路径}

验证由两条轨道交叉完成。规则层做确定性检查，覆盖六类已知扰动：数值篡改、限定词缺失、口径偷换、来源降级、时间错位、证据缺失。LLM 层做语义判断。没有配置 LLM 时，系统自动降级为纯规则路径，给出保守判定或拒答，而不是报错退出。规则层的结论在 LLM 之前执行，检出口径偷换这类硬问题时直接覆盖模型判断。

\section{证据账本与人机分工}

每条证据落库前生成内容指纹，重复写入自动跳过。人工终验是硬门控：写库工具要求来源、页码定位、原文摘录、口径说明、核验人五个字段齐全，核验人为空直接拒收。机器负责初核、留痕和口径审查，人负责终验和口径裁定。2026 年 9 月 9 日完成的 11 条 Claim 升级走的就是这个流程：机器在巨潮资讯网逐条找出公告原文并比对数值，人抽读确认后写回。

\section{能力边界}

当前版本是离线批处理 Agent，不做实时检索；证据来自本地缓存的公开披露文件，这是有意的设计——避免外部服务成为演示单点，保证任何环境下可复现。实时检索在路线图的放大期。

\section{与赛事评分维度的对照}

\begin{center}
\begin{tabularx}{\textwidth}{@{}lX l@{}}
\toprule
\textbf{评分维度} & \textbf{本项目的对应} & \textbf{证据位置} \\
\midrule
技术创新 30\% & 验证与估值的计算级传导、因果批判层、工程口径归一化、双轨验证与降级路径 & 第 2、3、5 章 \\
落地价值 40\% & 两家公司全链路实证、离线可复现、140 项回归、延迟实测 & 第 5、6、8 章 \\
商业潜力 30\% & 三类客户分层、按证据深度三档定价、案例报告获客路径 & 第 9 章 \\
\bottomrule
\end{tabularx}
\end{center}

% ================= 第 4 章 =================
\chapter{证据网络：51 条 Claim 的验证台账}

证据网络覆盖机器人关节模组产业链 11 家上市公司，共 51 条结构化 Claim。每条 Claim 的证据带五个必填字段：来源、页码定位、原文摘录、口径说明、核验人。核验人为空的证据，写库工具直接拒收。

\docfig{fig_claims.pdf}

公告原文、券商研报、媒体转述、行业报告在系统里是四个不同的证据等级，不许混用：研报转述的数字必须降级标注，找不到官方出处的条目维持「待验证」、不得升级。2026 年 9 月 9 日的一轮终验里，11 条 Claim 经巨潮资讯网公告原文逐字核对后升级为已验证，其中包括 SH\_003 的口径纠偏（见第 1 章案例）。

剩下 4 条待验证，原因各不相同：步科股份两条（BK\_001、BK\_003）的数值只在第三方蓝皮书和券商研报里出现，公司官方从未披露；绿的谐波 LHS-32（GH\_007）和双环 SHPR-20E（SH\_002）的逐型号参数表需要厂商选型手册才能核对，官网和招股书都没有文本层的参数表。这 4 条在报告里只以「待验证」身份出现，不进入估值输入。

% ================= 第 5 章 =================
\chapter{深度案例：一条 Claim 的完整处理过程}

以绿的谐波「关节模组减重 30\%」这条宣称为例，完整走一遍流水线。整个过程在本地离线实跑，无需联网。

输入后，规则引擎比对公司宣称与原始披露，检出限定词缺失：原文是「同等出力情况下减重 30\%」，宣称把前提丢了。判定部分成立，置信度 0.3。「部分成立」在门控的放行集合内，进入财务链路。随后 14 行工程参数做归一化：4 行可比，10 行明确标记为不可比。

经济映射阶段，工程参数被翻译成两条定量假设：扭矩密度映射到渗透率，调整 -10.26\%，证据等级 high；重量映射到 BOM 单位成本，调整 -5.47\%，弹性系数按环动科技招股书的材料成本证据校准过（精密减速器成本中加工和装配占大头，钢材只占很小一块，重量弹性从 0.5 下调到 0.25）。

随后因果批判层对这两条假设分别生成替代解释和待补反事实（共 12 条替代解释、14 条反事实需求），并把置信度按下限压到 0.6 和 0.32：

\docfig[0.72]{fig_confidence.pdf}

批判层调整后的假设进入 DCF，输出三情景企业价值：

\begin{center}
\begin{tabular}{@{}lccc@{}}
\toprule
情景 & \textbf{base 基准} & \textbf{upside 乐观} & \textbf{downside 悲观} \\
\midrule
企业价值 EV & 43.4 亿 & 69.4 亿 & 15.8 亿 \\
\bottomrule
\end{tabular}
\end{center}

悲观情景常驻输出，它回答的是「如果销量、价格、成本同时往坏的方向走，这家公司还剩多少」。

% ================= 第 6 章 =================
\chapter{通用性：同一条链路换一家公司}

同一条处理链路，换一组输入文件就是另一家公司。双环传动的移植如下：

\begin{center}
\begin{tabularx}{\textwidth}{@{}lX X@{}}
\toprule
 & \textbf{绿的谐波} & \textbf{双环传动} \\
\midrule
输入 & 绿的案例数据包 & 双环案例数据包（rv/gear 双产品线） \\
产品线识别 & 谐波 / 关节模组 & RV / 齿轮，自动识别 \\
映射规则 & 默认规则集 & 双环变体（规则可限定产品线作用域） \\
三情景 EV & 43.4 / 69.4 / 15.8 亿 & 72.7 / 122.9 / 19.0 亿 \\
\bottomrule
\end{tabularx}
\end{center}

移植过程中发现过一个真实的问题：模型最初给 RV 减速器设的 ASP 是 4,300 元/台，而环动科技（双环控股子公司）科创板招股书披露的实际均价是 2,533--3,209 元/台，且逐年下降。参数按招股书校准到 3,100 元之后，估值的锚点才落在证据上。

\docfig{fig_rv_anchor.pdf}

产品线泛化、规则集变体、作用域限定都是代码层实现，140 项回归测试锁定。第三家公司起的移植是工程化复制，成本主要是整理该公司的财务锚点，约一到两天。

% ================= 第 7 章 =================
\chapter{反向 DCF：模型 EV 与市值差距的处理方式}

简化 DCF 只计算可验证证据支持的基本面价值，而市值里还包含叙事预期。两者差距不回避、也不硬凑，做法是反向量化。

以绿的谐波 2026 年 9 月 4 日市值 512.96 亿元为起点反推：现价隐含的销量预期是可验证基准假设的 16.7 倍（实测 16.66$\times$）。换句话说，市场为人形机器人量产期权支付的价格约为 482 亿元，这部分不在 2025--2027 年可验证现金流里。

\docfig{fig_reverse_dcf.pdf}

一个值得记录的修正：2026-09-09 的参数复核把税率、单位成本、RV 均价全部按年报和招股书重新锚定之后，隐含倍数从 8.5$\times$ 扩大到 16.7$\times$。差距拉大说明原假设偏乐观，按证据修正让「市场有多乐观」有了更诚实的答案。

这张「隐含预期 ↔ 已验证证据」对照表就是交付物。我们不替买方下结论，把市场定价里隐含的预期拆开摆平，赔率由买方自己判断——所有估值输出常驻「原型情景、非投资建议」标注，这是产品和荐股类工具的分界线。

% ================= 第 8 章 =================
\chapter{工程可信度}

系统的可信建立在三个可检查的机制上：每个数字可复算（golden file 回归锁定，重跑结果不一致就报错）、每个断言可回溯（证据指纹加公告页码）、每个边界如实标注（降级路径、口径疑点、待验证清单全部写进输出）。

\begin{center}
\begin{tabularx}{\textwidth}{@{}llX@{}}
\toprule
\textbf{指标} & \textbf{数值} & \textbf{来源} \\
\midrule
回归测试 & 140 项全过 & \texttt{pytest tests -q} 实跑输出 \\
项目书一致性审校 & 0 issue & \texttt{scripts/audit\_proposal.py} 实跑输出 \\
规则链延迟 & p50 4.3ms / p95 5.0ms & \texttt{benchmarks/latency\_report.md}（N=30，2026-09-07 实测） \\
判别准确率 & 91.8\% / 90.8\% & \texttt{benchmarks/pipeline\_report.md}（98 用例） \\
\bottomrule
\end{tabularx}
\end{center}

判别准确率这组数字值得展开：98 个变异测试用例上，裸 LLM 的判别准确率是 62.5\%（Claude）和 65.3\%（GPT），叠加规则层后升到 91.8\% 和 90.8\%，规则层误伤为 0。这些数字衡量的是既有测试集上的判别能力，不是现实业务准确率。早期规则层曾有 6 次误伤（把正确 Claim 误判为有问题），修复后每次误伤类型都有可定位的回归用例，再犯会直接挂测试。

\docfig[0.72]{fig_benchmark.pdf}

% ================= 第 9 章 =================
\chapter{商业潜力}

\section{市场分层}

TAM 的上限锚是谐波减速器环节的远期空间 71--603 亿元。这里有意并列 GGII 和高盛两种口径——两个数差近 10 倍，选一个呈现反而是误导。SAM 按三类客户切分：买方研究员（判断赔率）、卖方研究员（报告质检）、产业资本（供应商与竞品核验）。SOM 按证据服务订阅估算，每年 2--6 亿元。

\docfig[0.9]{fig_tam.pdf}

\section{现有工具的缺口}

金融数据终端和通用 AI 投研工具普遍有四类缺口：验证和估值是两套数字（证据分级传导缺失）；数字找不到出处（证据账本缺失）；相关被当成因果（批判层缺失）；额定值和峰值混着比（工程口径归一缺失）。这四类缺口对应本产品的四个模块，也是定价的依据。

\section{定价与落地路径}

按证据深度分三档订阅：基础验证、证据链报告、定制链路。落地路径分三步：用绿的谐波案例报告获客，嵌入买方研究员的日常核验工作流，再 API 化输出。验证期目标是 5 家付费试点。

% ================= 第 10 章 =================
\chapter{路线图}

\docfig[0.92]{fig_roadmap.pdf}

% ================= 附录 =================
\appendix

\chapter{附录}

\section{Claim 验证分级统计（2026-09-09 终验后）}

\begin{center}
\begin{tabular}{@{}lrl@{}}
\toprule
\textbf{状态} & \textbf{条数} & \textbf{说明} \\
\midrule
已验证（公告原文级） & 47 & 含 11 条经巨潮公告原文逐字核对升级 \\
待验证 & 4 & 见第 4 章原因说明 \\
\midrule
合计 & 51 & 覆盖 11 家上市公司 \\
\bottomrule
\end{tabular}
\end{center}

\section{运行方法}

\begin{verbatim}
pip install -r requirements.txt
python app.py                    # 离线全链路 Demo（无需 API key）
python -m pytest tests -q        # 140 项回归测试
python scripts/audit_proposal.py # 项目书一致性审校
\end{verbatim}

代码、数据与文档：\url{https://github.com/aeiou0123/pku-financial-ai-agent}

\vspace{1cm}
\begin{warnbox}[免责声明]
本文所有估值均为研究用途的原型情景，不构成投资建议。所述三情景企业价值基于公开披露信息与显式标注的人工假设，参数口径见仓库 \texttt{data/processed/} 下各输入表的 notes 字段。本文部分内容由 AI 辅助生成，关键数字均经人工核对与回归测试锁定。
\end{warnbox}

\end{document}
